# Full-Bleed Black Beds — current

All four panes read edge-to-edge. Cream stripes survive only where a
readable bed is required (Students panes, Setup month bed).

## Current
- **Backup:** flush black bed, both panels edge-to-edge, no outline, no
  ring (pellet snake fully removed: no SVG, no WAAPI/SMIL, no `::before`).
- **Setup:** full-screen cream wall, no margin, no outline; toolbar +
  month flush to edges. Outer gutters black via scroller bed.
- **Attendance:** outlined wall on a black scroller bed.
- **Students:** black split-view gutters; list/detail panes keep opaque
  cream backing so dark text stays readable.
- Rail keeps its dashed left seam; it is not a ring and is untouched.

## Notes
- Panels keep their surfaces; only beds/gutters went black.
- Audit containment (`max-width:100%` + `overflow:hidden`) stays so
  black table paint can't bleed past the wall edge.
