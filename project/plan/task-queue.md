# Task Queue — current

Prior queue resolved: glass confirm shipped; `bg-spheres.jpg` and
`assets/` deleted; scheduler fields text-first; Setup inputs done;
UI pushed (`e99afcf` on `feature/ui-glass-redesign`).

## Current
- Pellet ring era closed: snake removed outright (no SVG, WAAPI/SMIL,
  or corner layers remain); Backup is a flush black bed.
- Black beds: Backup flush, Setup full-screen, Attendance outlined on
  black, Students black gutters + cream panes.
- Row-tap preview vs pen-edit split shipped (E2E-pinned).
- Shift-proof states shipped (inset markers, uniform weights).

## Open
- Nothing queued — owner picks the next task.
