# Toolbar Fields — current

Toolbar inputs/selects rest borderless (translucent fill, transparent
underline); ink hairline appears on hover/focus only. Pointer cursor
on selects; placeholders ink-70.

## Current
- `option` rows themed paper/ink (native popup parts stay OS-drawn —
  disclosed limit, not fixable in CSS).
- Dropdown triggers rendered by the custom-dropdown component.

## Notes
- Native `<select>`s kept live underneath (E2E `selectOption` safe).
