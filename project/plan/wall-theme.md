# Wall Theme — current (supersedes frost)

Black-and-cream wall on branch `feature/ui-glass-redesign`:
`#141414` / `#F4EEE1` / `#8A3A3A` destructive-only. Frost variables are
retired to solid cream (`--ref-frost: #F4EEE1`, blurs off).

## Current
- Glyph actions (red cross closes, black tick confirms, red trash
  far-left), cube fields (cream fill, 3px black outline), 4px scrollbars.
- Ambient image retired: `bg-spheres.jpg` deleted, `assets/` removed.
- No hover, no motion anywhere (the pellet snake was exempted, then
  removed outright with its ring).
- On conflict, the wall beats older frost prose. Untouched surfaces
  (kiosk/idle, print) keep prior language.

## Notes
- Single source of truth: `docs/UI_TOKENS.md` wall section +
  `docs/UI_COMPONENTS.md` wall logs.
